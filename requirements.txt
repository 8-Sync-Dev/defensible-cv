# Core requirements for rendering the CV.
rendercv[full]>=2.8

# Research crawler dependencies (scripts/research.py).
# Network I/O uses only the standard library (urllib) — no `requests` needed.
# PyYAML IS required: the crawler parses profile.yaml (its research_sources) and
# rendercv depends on it transitively too.
PyYAML>=6.0


# Per-page text extraction from citing-paper PDFs, so the CV can quote the
# exact sentence + page where another author invokes our work. Without pypdf
# the script still runs and reports each citing paper as "verified via
# citation graph", but the in-text excerpts are skipped.
pypdf>=4.0

# OPTIONAL: enables Google Scholar lookups via `python scripts/research.py`.
# Skip with `--no-scholar` if you don't want to install it (CI default).
# scholarly is famously flaky against Scholar's CAPTCHA, so we soft-fail.
# scholarly>=1.7
